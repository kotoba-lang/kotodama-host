# kotodama-host-sdk

CLJC facade artifact for the Kotodama host component contract.

The SDK surface is defined by `resources/kotodama_host/sdk_facade.edn` and
`src/host-contract.cljc`. TypeScript package metadata and runtime facade code
were removed; EDN/CLJC is the authority for host SDK operations.
